{
  "name": "project",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "build": "ponder codegen",
    "start": "ponder start --schema public",
    "tunnel": "npx localtunnel --port 42069 --subdomain astral-ponder",
    "tunnel-cf": "npx cloudflared tunnel --url http://localhost:42069",
    "tunnel-serveo": "ssh -R 80:localhost:42069 serveo.net",
    "tunnel-localhost": "ssh -R 80:localhost:42069 nokey@localhost.run",
    "tunnel-bore": "npx bore-cli 42069",
    "db": "ponder db",
    "codegen": "ponder codegen",
    "lint": "eslint .",
    "typecheck": "tsc"
  },
  "dependencies": {
    "ponder": "^0.15.10",
    "hono": "^4.5.0",
    "viem": "^2.21.3"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "eslint": "^8.53.0",
    "eslint-config-ponder": "^0.15.10",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.14"
  }
}